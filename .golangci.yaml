linters:
  disable-all: true
  enable:
    - gofumpt
    - staticcheck
    - govet
    - gci

linters-settings:
  gci:
    sections:
      - standard
      - prefix(github.com/javtube/metatube-sdk-go)
      - default
  staticcheck:
    go: '1.19'
